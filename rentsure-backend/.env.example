# Supabase PostgreSQL Database Credentials
# IMPORTANT: Use the SESSION-MODE pooler (port 5432) NOT transaction mode (port 6543)
DB_URL=jdbc:postgresql://db.nxujvinvafvfsavdlqwj.supabase.co:5432/postgres?sslmode=require
DB_USERNAME=postgres
# Replace this with your actual Supabase Database Password (NOT the API Secret Key)
DB_PASSWORD=your_actual_supabase_database_password_here

# JWT Secret for signing tokens (Must be at least 256 bits / 32 characters)
JWT_SECRET=super_secret_jwt_key_that_is_at_least_32_characters_long
